# 이 파일을 .env 로 복사한 뒤 값을 채우세요. (Git에는 .env 가 올라가지 않습니다)
# 로컬 개발: MariaDB/MySQL 주소 입력
# 시놀로지 NAS 배포: NAS의 MariaDB 주소와 비밀번호 입력

# MariaDB 연결 (phpMyAdmin에서 만든 DB 이름·사용자 사용)
# 형식: mysql://사용자명:비밀번호@호스트:3306/데이터베이스명
DATABASE_URL="mysql://golf_user:비밀번호@192.168.0.10:3306/golf_db"

# 앱 로그인 비밀번호
GOLF_PASSWORD=비밀번호입력

# (선택) 홈 화면에서 스코어(라운드) 삭제 시 요구할 비밀번호. 미설정 시 GOLF_PASSWORD 사용
# GOLF_DELETE_PASSWORD=삭제전용비밀번호

# (선택) 업로드 파일을 NAS 특정 경로에 저장할 때만 설정
# 예: UPLOAD_DIR=/volume1/docker/golf/uploads
# 미설정 시 컨테이너 내부 /app/public/uploads 사용 (볼륨 golf-uploads에 유지)

# (권장) 카카오톡·링크 공유 시 이미지가 깨지지 않도록 공개 접속 주소 설정
# NAS 배포 시 예: https://yoonha.synology.me:3001 또는 http://yoonha.synology.me:3001
# 프로토콜 포함, 끝에 슬래시 없이 입력
# NEXT_PUBLIC_APP_URL=https://yoonha.synology.me:3001
